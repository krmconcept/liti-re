<div class="page-content">
  <div class="container" style="max-width:800px">
    {% render 'breadcrumb' %}
    <h1>{{ page.title }}</h1>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</div>
